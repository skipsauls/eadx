({
	setup: function(component) {
        console.warn("ltng2waveHelper.setup: ", component);
		var recordId = component.get("v.recordId");
        console.warn("ltng2waveHelper.setup: recordId: ", recordId);
                
	}
})