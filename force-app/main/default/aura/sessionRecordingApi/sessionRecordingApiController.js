({
	createSession: function(cmp, event, helper) {
		helper.createSession(cmp, event);
	},
	updateSession: function(cmp, event, helper) {
		helper.updateSession(cmp, event);
	},
	deleteSession: function(cmp, event, helper) {
		helper.deleteSession(cmp, event);
	},
	getSessions: function(cmp, event, helper) {
		helper.getSessions(cmp, event);
	},
	getRecordedSessionTree: function(cmp, event, helper) {
		helper.getRecordedSessionTree(cmp, event);
	},
	getSessionPhrases: function(cmp, event, helper) {
		helper.getSessionPhrases(cmp, event);
	},
	createPhrase: function(cmp, event, helper) {
		helper.createPhrase(cmp, event);
	},
	updatePhrase: function(cmp, event, helper) {
		helper.updatePhrase(cmp, event);
	},
	deletePhrase: function(cmp, event, helper) {
		helper.deletePhrase(cmp, event);
	}
})