global class CaseUtilController {

/*
 * Commented out for deployment to EADX
 *
 
    @AuraEnabled
    global static String lookupCaseIdByExternalId(String id) {
    
        Case c = [SELECT Id FROM Case WHERE Case_ExternalId__c =: id];
        
        return c.Id;
    }
*/
}